import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-invalid',
  imports: [RouterLink],
  templateUrl: './invalid.html',
  styleUrl: './invalid.css'
})
export class Invalid {

}
